{
  "name": "money-tracking-backend",
  "version": "1.0.0",
  "main": "server.js",
  "engines": {
    "node": "20.12.0"
  },
  "scripts": {
    "start": "node server.js",
    "start:dev": "nodemon server.js",
    "test": "mocha --exit --recursive --timeout 10000",
    "test:auth": "mocha --exit test/auth.test.js --recursive --timeout 10000",
    "test:user": "mocha --exit test/user.test.js --recursive --timeout 10000",
    "test:wallet": "mocha --exit test/wallet.test.js --recursive --timeout 10000",
    "test:category": "mocha --exit test/category.test.js --recursive --timeout 10000",
    "test:transaction": "mocha --exit test/transaction.test.js --recursive --timeout 10000",
    "test:financialPlan": "mocha --exit test/financialPlans.test.js --recursive --timeout 10000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@veryfi/veryfi-sdk": "^1.3.2",
    "bcrypt": "^5.1.1",
    "cloudinary": "^2.4.0",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "date-fns": "^4.1.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "lodash": "^4.17.21",
    "mongoose": "^8.5.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "chai": "4.5.0",
    "chai-http": "4.4.0",
    "mocha": "^10.7.3",
    "nodemon": "^3.1.4"
  }
}
